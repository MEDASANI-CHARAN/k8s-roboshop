apiVersion: v1
kind: Pod 
metadata:
  name: nginx
  labels:
    purpose: demo
spec: 
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80

# ---

# apiVersion: v1
# kind: Pod 
# metadata:
#   name: almalinux
#   labels:
#     purpose: demo-1
# spec: 
#   containers:
#   - name: almalinux
#     image: almalinux:9
#     command: ["sleep", "1000"]
